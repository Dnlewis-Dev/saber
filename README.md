Chat UI prototype for web development course.

Put API KEY in line 10 of api.js

## Requirements covered

- ESLint configured (no errors when running on repo JS)
- Prettier configured (formatting rules enforced)
- VS Code settings for format-on-save
- Static UI in `index.html`

## Scripts

```bash
npm run lint
npm run format
```
